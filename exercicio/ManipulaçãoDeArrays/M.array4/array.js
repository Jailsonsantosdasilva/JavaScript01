// splice - usado para adicionar, remover ou substituir elementos em qualquer posição do array.


let arr = [1, 2, 3, 4, 5];

arr.splice(2, 0, 999)

// remover item

arr.splice(4,1)

// indexOf - usado para encontrar a posição (índice) de um elemento dentro de uma string ou de um array.

console.log(arr.indexOf(5))