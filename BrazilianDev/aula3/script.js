// For/Of Loop

const nomes = ["joao", "lucas", "maria", "ana"];

for (let nome of nomes){
    console.log(`se chama: ${nome}`)
}

// exemplo da aula
const ListaDeEstados = ["RJ", "SP", "MG"];

for (let estados of ListaDeEstados){
    console.log(`é um estado ${estados}`)
}

// outro exemplo

const MyName = ["Jailson"];
for (let meuNome of MyName){
    console.log(`Meu nome e ${meuNome.toUpperCase()}`)
}